<h1>Employees page</h1>

<% if @employees.any? %>
<table>
    <thead>
        <tr>
            <th>Last name</th>
            <th>Other names</th>
            <th>Role</th>
            <th>Employed from</th>
            <th>Employed to</th>
        </tr>
    </thead>
    <tbody>
        <% @employees.each do |e| %>
            <tr>
                <td><%= e.last_name %></td>
                <td><%= e.other_names %></td>
                <td><%= e.role %></td>
                <td><%= e.employed_from %></td>
                <td><%= e.employed_to %></td>
                <td>
                    <%= link_to "Change", employee_path(e) %>
                    <%= link_to "Remove", employee_path(e), method: :delete, data: { confirm: "Are you sure? Only employees who " } %>
                </td>
            </tr>
        <% end %>
    </tbody>
</table>
<% else %>
    <p>No employees to show</p>
<% end %>

<%= link_to "Add new employee", new_employee_path %>

<%= form_tag finish_path do %>
    <%= check_box_tag :confirm_validity %>
    <%= label_tag :confirm_validity, "I confirm this is all right"%>
    <%= button_tag "Finish and send" %>
<% end %>